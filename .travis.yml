language: node_js

node_js:
  - '8'

branches:
  only:
  - master
  - develop
  - v2

before_install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

install:
  - npm install

script:
  - 'if [ "$TRAVIS_PULL_REQUEST" = "false" ]; then npm run test:scripts; fi'
  - 'if [ "$TRAVIS_PULL_REQUEST" = "false" ]; then bash <(curl -s https://codecov.io/bash) -F unit; fi'
  - npm run test:styles
